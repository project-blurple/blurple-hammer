{
	"name": "Blurple Hammer Dev Container",
	"build": {
		"dockerfile": "Dockerfile"
	},

	"customizations": {
		"vscode": {
			"extensions": [
				"dbaeumer.vscode-eslint",
				"christian-kohler.npm-intellisense"
			]
		}
	},

	"forwardPorts": [9123, 9124],

	"portsAttributes": {
		"9123": {
			"label": "Appeals",
			"onAutoForward": "silent"
		},
		"9124": {
			"label": "Staff Portal",
			"onAutoForward": "silent"
		}
	},
	"otherPortsAttributes": {
		"onAutoForward": "ignore"
	},

	"onCreateCommand": "docker run -p 27017:27017 -v /workspaces/blurple-hammer/database:/data/db --name db -d mongo:4",// && cp ./.devcontainer/development.env ./.env",
	"updateContentCommand": "npm ci",
	"postStartCommand": "docker start db",

	"remoteUser": "node",
	"features": {
		"docker-in-docker": "latest"
	}
}